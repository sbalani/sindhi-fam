comment on column public.family_members.lived_locations is
'Ordered residence objects with structured place data, a residenceId, and optional numeric startYear and endYear values. Legacy objects without dates remain valid.';
